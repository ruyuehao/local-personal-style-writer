# ============================================================
# 训练环境依赖（云端 GPU / 魔搭 Notebook，Ubuntu + CUDA 12.4）
# ------------------------------------------------------------
# 版本按 2026-07-12 训练时「实际可用 / 实测」锁定，使重训可一键复现。
# 训练日当时最新稳定：LLaMA-Factory v0.9.5（2026-05-30，Latest，支持 Transformers v5）
# 安装顺序见 FINETUNE_GUIDE.md 1.1 节。
# 注意：本文件只管「依赖版本」；LLaMA-Factory 因其从源码安装，单独用 git clone -b 锁定。
# ============================================================

# ---- PyTorch（CUDA 12.4，匹配魔搭 Notebook）----
# 使用阿里云 PyTorch wheel 镜像（公网备用：https://mirrors.aliyun.com/pytorch-wheels/cu124/）
# 若默认源找不到 +cu124 后缀 wheel，加 -f 指定索引：
#   pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 torchaudio==2.6.0+cu124 \
#     -f https://mirrors.aliyun.com/pytorch-wheels/cu124/
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# ---- LLaMA-Factory（QLoRA 微调框架）----
# 训练实测版本：LLaMA-Factory 0.9.6.dev0（main 快照，晚于稳定版 v0.9.5），见 FINETUNE_GUIDE.md 1.1.3 验证记录。
# ⚠️ 原命令用 `git clone --depth 1`（main 快照）不可复现。复现训练请锁定最新稳定 tag v0.9.5
#    （2026-05-30，Latest，支持 Transformers v5，最接近实测 0.9.6.dev0）：
#   git clone -b v0.9.5 --depth 1 https://github.com/hiyouga/LLaMA-Factory.git
#   cd LLaMA-Factory && pip install -e ".[torch,metrics]"
# （不放 `llamafactory==0.9.5` 进 pip 依赖：它走源码安装，避免与 git tag 漂移）

# ---- OpenVINO 栈（导出 INT4 / INT8 时使用，与端侧推理版本严格一致）----
openvino==2026.2.1
openvino-tokenizers==2026.2.1.0
optimum[openvino]==2.2.0
optimum-intel==2.0.0
nncf==3.2.0

# ---- HuggingFace 生态（与端侧推理版本对齐，避免导出/加载权重不一致）----
transformers==5.0.0
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==1.23.0

# ---- QLoRA 量化后端（Linux CUDA 环境）----
# bitsandbytes 由 LLaMA-Factory 的 [torch] extra 拉取；如需显式锁定，按训练日可用版本加：
#   bitsandbytes==0.45.0
# （版本未钉死：训练环境经 LLaMA-Factory 依赖解析，云端 Linux CUDA 用官方 wheel 即可）
